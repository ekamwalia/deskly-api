$kind: http-request
name: 'localhost:4000/bookings/:bookingId'
description: 'Fetch a single booking by its ID.'
url: '{{baseURL}}/bookings/:bookingId'
method: GET
auth:
  type: apikey
  credentials:
    key: X-API-Key
    value: demo-key
params:
  - type: path
    key: bookingId
    value: bkg_seed01
    description: 'The ID of the booking to fetch.'
order: 1787037123670
